<template>
  <div>
    <praeco-form-item label="pagertree_integration_url" prop="pagertreeIntegrationUrl">
      <el-input v-model="pagertreeIntegrationUrl" :disabled="viewOnly" />
      <label>URL generated by PagerTree for the integration.</label>
    </praeco-form-item>

    <praeco-form-item label="pagertree_proxy" prop="pagertreeProxy" required>
      <el-input v-model="pagertreeProxy" :disabled="viewOnly" />
      <label>
        By default ElastAlert 2 will not use a network proxy to send notifications to PagerTree.
        Set this option using hostname:port if you need to use a proxy.
      </label>
    </praeco-form-item>
  </div>
</template>

<script setup>
import { useConfigAlertPagerTree } from '@/composables/config/alert/useConfigAlertPagerTree';

defineProps({
  viewOnly: Boolean
});

const {
  pagertreeIntegrationUrl,
  pagertreeProxy
} = useConfigAlertPagerTree();
</script>
