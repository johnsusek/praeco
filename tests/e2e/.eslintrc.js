module.exports = {
  // Cypress plugin removed for Vue 3 migration
  // plugins: ['cypress'],
  env: {
    mocha: true,
    // 'cypress/globals': true
  },
  rules: {
    strict: 'off',
    'max-len': 'off'
  }
};
